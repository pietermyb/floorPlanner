# floorPlanner

Ionic and Electon based Floor Planner with Meta data recorder.

# Serving the app in a browser

Run `ionic serve` in the console from the root directory

# Serving the app on Android

Run `ionic cordova run android` to run the app on a Developer enabled Android device with USB debugging enabled



